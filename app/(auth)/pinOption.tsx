import { View, Text } from "react-native";
import React from "react";

const pinOption = () => {
    return (
        <View>
            <Text>pinOption</Text>
        </View>
    );
};

export default pinOption;
